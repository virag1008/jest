import { render, screen, logRoles } from "@testing-library/react";
import App from "./App";

// test("test 1", () => {
//   render(<App />);
//   logRoles(screen.getByTestId("myrootdiv"));
//   const buttonEle = screen.queryByRole("button", {
//     name: "test Button",
//     exact: false,
//   });
//   expect(buttonEle).toBeInTheDocument();
//   // expect(buttonEle).not.toBeInTheDocument();
//   // expect(buttonEle).toBeNull();
// });
